experiment_name: "3D_Hemorrhage_HybridSegFormer_UMamba"
fold: 0
seed: 42
deterministic: true
epochs: 5
batch_size: 2
num_workers: 4
patience: 30
mixed_precision: true
amp_dtype: "bfloat16"
compile_model: false
channels_last: false
grad_accum_steps: 8
max_grad_norm: 1.0
log_freq: 10
save_dir: "checkpoints/phase2_smoke_test"

# Phase 4.3: Health monitor enabled to track gradient norms
health_monitor:
  enabled: true
