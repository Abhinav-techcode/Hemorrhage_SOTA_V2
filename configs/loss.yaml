# Loss Ablation Framework
weighting_strategy: "learnable"  # 'static', 'learnable', 'uncertainty'

loss:
  dice:
    enabled: true
    weight: 1.0
    params: { sigmoid: true, squared_pred: true }
  
  focal:
    enabled: false
    weight: 1.0
    params: { gamma: 2.0, alpha: 0.75 }
    
  tversky:
    enabled: true
    weight: 1.0
    params: { sigmoid: true, alpha: 0.7, beta: 0.3 }
    
  boundary:
    enabled: true
    weight: 0.5
    params: {}

deep_supervision:
  weights:
    full: 1.00
    half: 0.50
    quarter: 0.25